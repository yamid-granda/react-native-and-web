services:
  postgres:
    image: postgres:17-alpine
    restart: unless-stopped
    environment:
      POSTGRES_USER: rnw
      POSTGRES_PASSWORD: rnw
      POSTGRES_DB: rnw_dev
    ports:
      - "5432:5432"
    volumes:
      - pgdata:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U rnw -d rnw_dev"]
      interval: 5s
      timeout: 5s
      retries: 10

volumes:
  pgdata:
